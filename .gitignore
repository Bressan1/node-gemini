# Arquivos e pastas que não devem ir para o Git

# Variáveis de ambiente (contém chaves secretas)
.env

# Dependências
node_modules/

# Utilitários locais (não necessários no repo)
src/utils/